pipeline {
    agent any
    options {
        // Timeout counter starts AFTER agent is allocated
        
    }
    stages {
        stage('Example') {
            steps {
                echo 'Deployment success'
            }
        }
    }
}
